# ── 敏感資訊（絕對不能進 repo）────────────────────────────
.env
*.pem
*.key

# ── 每個人本機不同，不應該同步 ────────────────────────────
.DS_Store
Thumbs.db

# ── Docker 產生的資料，不進 repo ───────────────────────────
n8n_data/
para-ai-system/n8n_data/

# ── 原始對話 JSON（隱私，永遠本地保留）────────────────────
para-ai-system/vault/raw/

# ── 生成的媒體檔案（太大，不進 repo） ─────────────────────
*.mp3
*.mp4

# ── Python ────────────────────────────────────────────────
__pycache__/
*.pyc

# ── Node.js ───────────────────────────────────────────────
node_modules/
